
import {IFeedbackScreenStyleProps} from './FeedbackScreen.types';
import {StyleSheet} from 'react-native';

export const generateStyles = (props: IFeedbackScreenStyleProps) => {
  const {} = props || {};
  
  return StyleSheet.create({
    mainContainer: {},
  });
};
