import {IHelpScreenStyleProps} from './HelpScreen.types';
import {StyleSheet} from 'react-native';

export const generateStyles = (props: IHelpScreenStyleProps) => {
  const {} = props || {};

  return StyleSheet.create({
    mainContainer: {},
  });
};
