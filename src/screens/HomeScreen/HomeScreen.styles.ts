import {StyleSheet} from 'react-native';

export const generateStyles = () => {
  return StyleSheet.create({
    mainContainer: {
      padding: 12,
    },
    profileContainer: {
      marginBottom: 10,
    },
    Container: {},
  });
};
