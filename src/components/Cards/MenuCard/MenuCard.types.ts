export interface IMenuCardProps {
  image?: string;
  icon?: any;
  name: string;
  shouldFlex?: boolean;
  handleMenuCardPress?: () => void;
}

export interface IMenuCardStyleProps {
  shouldFlex?: boolean;
}
