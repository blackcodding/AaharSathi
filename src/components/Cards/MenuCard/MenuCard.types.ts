export interface IMenuCardProps {
  image?: string;
  icon?: any;
  name: string;
  shouldFlex?: boolean;
  handleMenuCardClick?: () => void;
}

export interface IMenuCardStyleProps {
  shouldFlex?: boolean;
}
