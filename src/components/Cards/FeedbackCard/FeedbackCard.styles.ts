import {IFeedbackCardStyleProps} from './FeedbackCard.types';
import {StyleSheet} from 'react-native';

export const generateStyles = (props: IFeedbackCardStyleProps) => {
  const {} = props || {};

  return StyleSheet.create({
    mainContainer: {},
  });
};
