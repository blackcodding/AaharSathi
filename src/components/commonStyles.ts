import {StyleSheet} from 'react-native';

export const commonStyles = StyleSheet.create({
  gifContainer: {
    width: 28,
    height: 28,
    marginRight: 4,
    borderRadius: 100,
  },
  gif: {
    width: '100%',
    height: '100%',
  },
});
